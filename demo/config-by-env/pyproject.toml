[project]
name = "config-by-env"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
]
[tool.pytest.ini_options]
markers = [
    "unit: unit tests",
    "smoke: subset of unit tests used for CI builds",
]