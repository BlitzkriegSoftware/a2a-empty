apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: agent2agent-prod
spec:
  template:
    spec:
      containers:
        - image: gcr.io/YOUR_PROJECT_ID/agent2agent
          ports:
            - containerPort: 8080
          env:
            - name: ENV_STAGE
              value: "prod"
            - name: AGENT_API_KEY_PROD
              valueFrom:
                secretKeyRef:
                  name: AGENT_API_KEY_PROD
                  key: latest
      serviceAccountName: github-deployer@YOUR_PROJECT_ID.iam.gserviceaccount.com
  traffic:
    - latestRevision: true
      percent: 100
